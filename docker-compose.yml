# docker-compose.yml

services:
  script-store:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./scripts-store:/opt/scripts-store
      - ./logs:/opt/logs
      - ./data:/opt/data
    env_file:
      - .env
    environment:
      - PYTHONPATH=/opt
    restart: unless-stopped